# First-slice token usage

The colors in `tokens.json` are semantic role tokens, not approved normal-text foreground pairs. A component must re-check a contrast ratio of at least 4.5:1 against its actual background before using any of these colors for normal text.

This protocol does not add component colors, neutral colors, radii, shadows, dark mode, or other later-slice tokens.
