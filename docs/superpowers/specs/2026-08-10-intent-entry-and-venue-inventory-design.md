# 目的入口与场馆单日库存切片设计

**日期：** 2026-08-10  
**状态：** 视觉方向已确认，等待书面规格确认  
**前序决策：** [三类用户与开放球局产品设计](./2026-08-09-three-sided-football-product-design.md)

## 1. 结论

小程序不要求用户注册时永久选择“球场方、队长或散客”。首次进入时先用三个同等权重的目的入口分流：

1. **我要出租场地**：申请合作，或进入已有权限的场馆工作台；
2. **我要租赁场地**：为球队寻找并预订整块场地；
3. **我要找球踢**：没有球队也可以浏览并申请加入已经锁定真实场地的开放球局。

用户以后再次打开小程序时进入综合首页。综合首页继续保留三个平权入口，同时展示最近一次任务和“我的”入口。用户选择的是当下目的，不是永久身份。

散客入口使用日常语言“我要找球踢”。进入该模块后，页面标题和领域对象再使用“找球局”“球局详情”和“申请加入”。

## 2. 为什么采用三个平权入口

采用用户确认的“三入口平权”方案，原因是：

- 首屏直接回答用户“我来这里要做什么”，比展示内部模块名称更容易理解；
- 同一个微信用户可以在不同时间拥有不同目的，不需要切换账号或角色；
- 三种目的都保持稳定位置，便于形成记忆；
- 副标题承担权限和业务边界说明，不让“出租场地”被误解为任何人都能立即发布。

未采用“踢球者优先、场馆方次级入口”，因为用户明确希望首屏像房产平台一样让不同目的并列。未采用纯频道切换器，因为它更像功能分类，弱化了首次进入时的意图引导。

## 3. 首次进入与回访首页

### 3.1 首次进入

目标 viewport 为 `375 × 812`，使用系统导航与安全区。页面只包含：

- 品牌识别；
- 标题“你今天想做什么？”；
- 一句说明“选择一个目的开始，之后可以随时切换”；
- 三张相同尺寸、相同层级的目的卡片；
- 说明“这里选择的是当下目的，不是永久身份”。

三张卡片的正式文案为：

| 入口 | 副标题 | 真实去向 |
| --- | --- | --- |
| 我要出租场地 | 申请合作，或进入已授权的场馆工作台 | 权限判断后进入工作台或合作接入说明 |
| 我要租赁场地 | 为球队查找时间、价格和可订整场 | 现有地图找场、时段、下单和支付旅程 |
| 我要找球踢 | 没有球队，也能加入已锁定场地的开放球局 | 找球局列表、详情、申请、审核或候补旅程 |

浏览球场和公开球局不要求先登录。进入场馆工作台、提交订单或申请加入时，再在动作边界完成身份验证。

### 3.2 回访首页

回访首页不是再次强制选择的门槛。它包含：

- 顶部“我的”入口；
- 三个同等尺寸的快捷入口：出租场地、租赁场地、找球踢；
- “继续上次”区域，只恢复最近浏览或操作的模块，不自动执行写操作；
- “我的进行中”摘要，只显示真实订单、报名或场馆待办。

没有进行中事项时直接省略该区域，不用模拟数据填充首页。首期不建设推荐流、社交动态、广告位或经营报表。

本地只保存最近使用的目的和可公开恢复的页面位置。订单、报名、场馆权限和待办数量必须来自服务端权威数据。

## 4. 下一条垂直切片

共享入口视觉确认后，下一条可闭环旅程为：

```text
已获授权的场馆工作人员
→ 我要出租场地
→ 场馆工作台
→ 选择日期和物理场地
→ 选择一个尚未售出的时段
→ 修改开放状态或价格
→ 确认保存
→ 用户端读取到更新后的真实库存
```

该切片只服务唯一 `ONLINE` 合作场馆“渤海元丰足球场”，不为四家 `DIRECTORY_ONLY` 场馆创建库存或管理员权限。

### 4.1 最小工作台页面

工作台默认打开今天的库存视图，页面只包含：

- 场馆名称；
- 日期选择；
- 五人制/七人制物理场地切换；
- 当日时段列表；
- 每个时段的开始结束时间、价格和状态；
- 未售时段的“调整价格”和“临时关闭/重新开放”操作；
- 保存中的反馈、成功反馈和可恢复错误。

首切片不包含批量开放、周期模板、场馆资料编辑、经营报表、官方球局或今日订单履约。这些属于后续独立旅程。

### 4.2 可修改边界

只有 `AVAILABLE` 或 `CLOSED` 且没有有效订单占用的未来时段可以修改：

- `AVAILABLE`：可以修改价格或临时关闭；
- `CLOSED`：可以修改价格并重新开放；
- `LOCKED`：展示“用户下单中”，不可修改；
- `BOOKED`：展示“已售出”，不可修改；
- 已经开始的时段：只读。

价格使用整数分保存，界面使用人民币元显示。保存时客户端提交用户明确修改的目标值，不根据旧页面状态自行推断成功。

## 5. 权限与权威边界

场馆方入口平权，不代表发布权限平权。

- `VenueMembership` 是场馆权限的服务端权威；
- 首期管理员由平台审核配置，不开放商家自助注册；
- 无场馆权限的用户进入合作接入说明，不能看到联系人、订单或库存编辑能力；
- 管理员只能操作 membership 绑定的 `venue_id`；客户端传入其他场馆 ID 不能扩大权限；
- 权限失效时，正在打开的编辑页必须在保存时得到明确拒绝并刷新为只读状态。

场馆内容资料仍由平台协助维护。该切片不增加名称、地址、图片、电话等 CMS 编辑权限。

## 6. 前端状态与失败处理

Artifact 和 Fixture 至少覆盖四个 `375 × 812` 状态：

1. 首次目的选择；
2. 回访综合首页；
3. 单日库存就绪及可编辑时段；
4. 时段编辑面板及保存中/保存失败反馈。

必要状态语义：

- 页面加载失败：保留页面结构，提供“重新加载”；
- 保存中：当前编辑操作不可重复提交，其他页面不伪造成功；
- 并发变化：显示“该时段状态已变化”，刷新权威状态；
- 权限不足：退出编辑态，说明需联系平台管理员；
- 无当日时段：显示真实空状态，不自动生成 Fixture 到生产包。

前端 Fixture 只存在于开发组合根，契约和后端集成完成后删除。生产构建不得包含场馆权限、库存或保存成功的模拟业务数据。

## 7. 视觉规则

沿用现有小程序设计系统，而不是采用新的营销型主题：

- 背景 `#F8FAFC`，表面 `#FFFFFF`，主文本 `#10243E`；
- 信任蓝 `#0284C7`，可用绿 `#059669`，锁定/注意状态使用既有语义；
- 系统字体、现有 `rpx` 字号和 8rpx 间距体系；
- 触控区域至少 `88rpx`；
- 使用统一 SVG/原生图标，不使用 emoji 充当功能图标；
- 三个目的入口的尺寸、字重和交互反馈相同；
- 状态不能只靠颜色表达，必须同时显示文字。

视觉验收在相同目标 viewport 下同时检查 reference、真实原生实现截图、并排图、50% 叠加图和差异图。用户明确确认前，不进入契约和后端阶段。

## 8. 交付顺序与上线门槛

按一个在制品推进：

1. 固化首次入口、回访首页和场馆库存的 Artifact；
2. 用隔离 Fixture 完成原生前端 Demo；
3. 完成同 viewport 视觉对比并取得用户确认；
4. 冻结场馆 membership、库存读取和单时段修改契约；
5. 实现场馆权限、并发保护和库存写入后端；
6. 前后端集成，验证用户端能读取更新后的库存；
7. 完成必要自动化与设备/用户验收；
8. 删除 Fixture。

三入口可以提前作为 Artifact 和开发预览存在，但生产首页只有在每个可点击入口都有真实、状态诚实的去向时才启用。若“找球踢”尚未实现，不用虚假球局填充，也不把不可用卡片伪装成已开放能力。

## 9. 轻量验证范围

前端阶段只验证：

- 三入口层级与文案；
- 目标 viewport 的几何和安全区；
- 日期/场地/时段主要交互；
- 保存中、失败、并发变化和权限不足的可理解反馈；
- Fixture 与生产包隔离。

后端阶段只对高风险边界增加必要测试：

- 场馆 membership 权限；
- 不能修改其他场馆或已锁定/已售时段；
- 并发修改不会覆盖订单状态；
- 价格与状态更新原子生效；
- 用户端读取结果与管理员保存结果一致。

不在本切片建设通用后台框架、复杂角色系统、批量排班引擎、审计分析平台或大规模模糊测试。
