# 三类用户平台整体切片路线图

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按依赖波次交付场馆、队长、散客和平台运营四个用户面的真实旅程，同时限制并行在制品。

**Architecture:** 微信小程序承载三种普通用户目的入口和业务旅程；FastAPI/PostgreSQL 提供身份、权限、库存、订单和球局权威状态；OSS 保存场馆媒体；后置的独立 Web 后台承载平台级加盟与认领审批。每个切片必须真实可部署、状态诚实且不含生产 Fixture 才算完成。

**Tech Stack:** 微信小程序 TypeScript/WXML/WXSS、FastAPI、SQLAlchemy、PostgreSQL、阿里云 OSS、DashScope、独立 Web 管理后台。

---

## 状态说明

- `已完成`：生产真实链路已经闭环并通过相应验收。
- `验收收口中`：实现已接入真实后端，仍有明确的设备验收或合并发布工作。
- `可用版本已交付 / 验收债延期`：生产能力已经实现并部署，可以继续后续 MVP 开发；明确列出的真实设备或数据库验收证据仍未通过，不等同于“已完成”。
- `下一项`：当前完成设计确认，随后进入实现。
- `待开发`：依赖前序切片，尚未开始生产实现。

## 波次 A：当前基础与入口

### A0：可扩展地图目录 — 已完成

真实场馆目录、地图搜索、行政区筛选、聚合标记和列表已经形成生产旅程，不再使用百条目录 Fixture 作为验收结果。

### A1：场馆资料、物理场地和库存工作台 — 验收收口中

已接入微信身份、场馆 membership、PostgreSQL、OSS、内容审核、物理场地和单场地库存。剩余工作只关闭已发现的真机交互/视觉问题，完成生产构建、Fixture 泄漏检查、真机验收、合并和部署。

### A2：三种目的生产入口与“我的场馆”组合页 — 验收收口中

- [x] 将已确认的三入口视觉移入生产包并设为小程序启动页。
- [x] “我要租赁场地”进入现有真实地图旅程。
- [x] “我要找球踢”显示“即将开放”，不进入 Fixture 或虚假列表。
- [x] 增加只读的“我的可管理场馆”接口，以服务端 membership 为权威。
- [x] 点击“我要出租场地”始终进入“我的场馆”，不因只有一个授权场馆而自动跳转。
- [x] “我的场馆”同时展示已授权场馆、创建新场馆、认领已有场馆三个稳定入口；选择场馆后才进入工作台。
- [x] 工作台提供返回“我的场馆”的稳定路径，让多场馆用户可以切换、新增或认领。
- [x] 零个、一个和多个授权场馆共用同一信息架构；不得让单场馆用户失去新增或认领入口。
- [x] 已完成聚焦路由、权限分支、生产构建和目标 iPhone viewport 验收。

A2 可以在不改变 A1 数据模型的前提下先实施。创建和认领按钮只有在 A3 的真实申请接口上线后才可进入生产包；视觉预览阶段使用隔离 Fixture，不用静态成功或 Toast 冒充提交。

### A3：场馆认领、新场馆加盟与平台审核 — 可用版本已交付 / CREATE 真机验收延期

普通小程序和独立 Web 后台共同组成一个完整垂直切片：

- [x] 小程序支持搜索并认领已有场馆，或提交尚未收录的新场馆加盟申请。
- [x] 新增独立申请数据和私有证明材料，不把未核验场馆提前写入公开 `venues`。
- [x] 创建申请在提交与批准两个边界分别执行去重检查；命中已有场馆时转入认领，不复制场馆。
- [x] 独立 Web 后台只接受平台级凭证，服务端校验 `PLATFORM_ADMIN` 或 `ONBOARDING_REVIEWER`；普通用户和场馆管理员均无权访问。
- [x] 审核人员可以查看证明、批准或拒绝申请，并记录操作者、时间和原因。
- [x] 批准已有场馆认领时只创建或激活目标 `venue_id` 的 `venue_memberships`。
- [x] 批准新场馆时在同一事务中创建 `is_listed=false` 的正式 `venues` 和首位管理员 `venue_memberships`。
- [x] 审核前、审核中和拒绝状态都诚实展示；任何申请提交都不能自动获得权限。

A3 前移到订单和球局切片之前。平台主动招商邀请与员工分级授权仍保留在后续扩展，不阻塞首个“用户申请—平台审核—获得权限”闭环。

截至 2026-08-18，生产数据库、私有 OSS、申请 API、小程序页面、平台审核后台和部署已经完成。真实 CLAIM 已在 iPhone 完成提交，经平台批准后核对数据库不变量通过：只产生目标场馆的一条有效可管理 membership，没有创建新场馆。

CREATE 的生产代码与部署同样存在，但根据用户 2026-08-18 的明确决定，其真实 iPhone 提交—平台批准—数据库不变量核对与对应设备验收暂缓。这项证据仍是已知验收债，不得记为通过，也不得将 A3 标记为“已完成”；恢复时只需补做一次受控 CREATE 真实链路及聚焦设备核对。

该延期债务不再阻塞 B1 MVP 开发。团队现在可以从可用版本继续补齐订单履约，并在后续迭代回收 CREATE 验收债。

## 波次 B：订单与组织者旅程

### B1：补齐订单履约 — 场馆退款已激活，原因退款真机验收待完成

- [x] 真实 owner-only“我的订单”列表、分页、刷新、错误重试和详情返回已部署到 staging。
- [x] 冻结订单取消、全额退款、场馆核销/完成和资金异常共用的 PostgreSQL 存储、纯策略、Provider 协议与静态 OpenAPI 契约。
- [x] 完成一次真实 iPhone“我的订单”刷新、卡片详情、终态回读和可见按钮验收；真实数据已显示“已经到底了”，没有可见 load-more，未人为制造网络失败，相关错误与分页状态由现有自动化覆盖并按比例性接受。
- [x] 使用体验版 `0.1.3` 在真实 iPhone 完成一次受控 CNY 0.01 JSAPI 支付与 owner 全额退款；支付收敛为 `Payment.SUCCESS / Order.CONFIRMED / Slot.BOOKED`，退款到账并收敛为 `Order.REFUNDED / Slot.AVAILABLE`，且只有一组付款与退款记录、无遗留 claim。
- [ ] 补做真实 Provider 回调的人工重复投递，以及强制主动查询/worker recovery 路径；本次正常通知已直接收敛，未捕获或重放原始回调，也未强制触发 recovery，不宣称这些路径已完成真商户验收。
- [x] 实现 owner 无资金取消生产路由、小程序动作和权威状态展示，并完成真实 iPhone 取消、详情重开、列表刷新和时段释放验收。
- [x] 实现场馆今日订单、签到和完成生产旅程，并通过真实 iPhone 零金额订单验收及两端刷新后的“已完成”权威状态回读。
- [ ] 完成场馆原因退款真机验收；受控 owner 资金 smoke 已通过，场馆退款 route 已在 staging
  revision `87da5d50` 激活并由匿名 `401` 证明已挂载，但尚未发送带身份的场馆退款请求。

2026-08-20，体验成员使用已成功上传的体验版 `0.1.1` 在受控 staging 完成 9 项联合 PASS。验收后已删除 my-orders 与 venue-fulfillment 的临时 Fixture/dev pages，以及 venue route fragment；生产页面、真实 HTTP composition、attempt store、生产 route、历史视觉证据和 audit deny rules 保留。`ONLINE_BOOKING_ENABLED=false`，`MINIPROGRAM_PAYMENT_PROVIDER=disabled`，因此本段状态不代表真实支付、退款、owner 取消或整个 B1 完成。

同日，体验成员又使用体验版 `0.1.2` 完成 owner 无资金取消 9/9 真机 PASS：待支付订单经真实确认变为 `CANCELLED`，列表和详情重开均稳定回读，目标 slot 回到 `AVAILABLE`，且 Payment/RefundCase/RefundAttempt 均为 0。验收数据随后按精确范围清理，身份图未改；`order-cancellation` Fixture、route fragment 和临时中央 hooks 已删除，生产订单页面、真实 HTTP 路由及 audit deny rules 保留。

2026-08-22，体验成员使用体验版 `0.1.3` 完成一笔 CNY 0.01 真实 JSAPI 支付及 owner 全额退款。支付通知直接将订单/库存收敛为 `CONFIRMED/BOOKED`，退款终态为 `REFUNDED/AVAILABLE`，微信账单确认全额原路退回。服务端最终核验只有 1 条成功 Payment、1 个退款 case 和 1 次成功 attempt，无活动 claim；专用测试 slot 随后由场馆操作员关闭，真实资金账本保留。没有保留或人工重放原始回调，也未强制触发主动查询/worker recovery。

同日，staging 后端升级到 `87da5d50cfdb70e954ec067dfb93c64a36718e5e`：仅在真实 WeChat
Provider 构造成功时挂载场馆退款路由并启用服务端 `can_refund` 投影。发布后 API/PostgreSQL
健康、worker/Caddy 运行、Alembic 保持 `0014`，匿名退款请求由部署前 `404` 变为 `401`；
未发送带身份的场馆退款请求，未触发第二笔真实资金变化，也未上传新的体验版。

共享基础只表示 revision `0013`、共享模型/策略、Provider 结果协议和公共契约已经冻结，不能把静态 OpenAPI 路径当作已上线能力。场馆今日订单、签到和完成 runtime 已在该冻结边界内落地；正常路径的真实资金支付与 owner 全额退款已完成受控验收，场馆退款生产路由已激活；人工重复回调、强制 recovery 与场馆原因退款真机资金验收仍由各自轨道继续完成。后续轨道不得修改 `backend/app/models.py`、revision `0013`、共享 Provider result enums 或冻结的公共 OpenAPI schemas；中央路由注册、production composition、build/audit 汇总继续由集成协调任务串行管理，本次 my-orders、venue-fulfillment 和 order-cancellation 临时 Fixture 均已完成删除。

这些能力共同为公开球局取消联动和真实履约提供基础。B1 真实 iPhone“我的订单”、无资金 owner 取消、正常路径真实微信支付、owner paid-refund terminal acceptance 及场馆签到/完成验收债已关闭；A3 CREATE 真机验收债保持不变。人工重复回调及强制 recovery 仍是明确证据缺口，场馆原因退款仍待真机资金验收，因此 B1 暂不标记为“已完成”；这些剩余项不阻塞后续非资金 MVP 开发。

### B2：队长开放名额 — 待开发，依赖 B1

已支付队长从真实订单创建球局，发布公开详情和分享卡片；一个订单最多对应一个未取消球局。

## 波次 C：散客旅程

### C1：散客申请与队长审核 — 待开发，依赖 B2

完成找球局、详情、申请、队长接受或拒绝、候补、关键状态通知和“我的报名”。完成后将入口页“我要找球踢”从“即将开放”切换为真实旅程。

### C2：退出、到场与治理 — 待开发，依赖 C1

增加退出、订单取消联动、队长记录到场/爽约、举报和平台人工纠错；不引入综合信用分或散客平台内分账。

## 波次 D：平台运营扩展

### D1：定向招商邀请与场馆员工授权 — 待开发，依赖 A3

- [ ] 平台主动招商时创建一次性、短期有效邀请，由场地方微信登录接受并补充资料；接受邀请本身不授予权限。
- [ ] 已认证负责人邀请场馆员工，并按资料、场地和库存范围授权。

本扩展不重复 A3 的加盟、认领和审批主链路，也不扩展经营报表、客服工单或综合运营后台。

## 依赖与并行边界

- A2 的小程序视觉预览与 A3 的独立 Web 审核预览文件互不重叠，可以并行；两者均须用户视觉确认后才进入契约和后端。
- A3 的申请后端与审核后端共享状态机和数据库事务，不并行修改同一模型；契约冻结后，小程序 HTTP 集成和 Web 后台 HTTP 集成可以并行。
- B1 的共享生命周期基础已冻结后，微信 Provider、owner 取消退款、场馆履约三条轨道可在各自文件边界内并行；它们只能消费共享模型、revision `0013`、Provider result enums 和公共 OpenAPI schemas，不得回写这些权威边界。
- B1 稳定后，B2 的前端 Artifact 与 B1 的非重叠验收可以并行；B2 后端不能绕过真实订单状态。
- C1 的前端视觉可在 B2 契约冻结后并行，真实申请后端必须等待 B2 发布模型稳定。
- D1 只在 A3 稳定后开始；它与 C 系列领域数据独立，但不阻塞当前加盟主链路。
- 每个切片继续遵循 Artifact/真实运行时预览、契约、后端集成、聚焦自动化、真机验收和删除 Fixture 的完成定义。
