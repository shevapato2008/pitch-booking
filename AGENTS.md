# Project Development Rules

## Visual self-review before user review

在把页面交给用户做视觉审核前，开发者必须先在目标真实运行时完成一轮人工视觉自审。至少检查按钮文字是否同时水平居中和垂直居中（不能只设置高度或依赖默认 `line-height`，应使用明确的布局对齐）、同组重复控件或状态徽标是否尺寸一致并沿同一列对齐、图标与箭头/X 的完整性、元素边界与裁切、固定底栏与安全区、关键文案和数据是否正确显示。存在一眼可见的问题时不得提交用户验收；应先做最小修复，避免把该要求扩张为过度设计、过度审核或过度测试。

## Every product button must work end to end

产品页面中的每一个按钮都必须有真实、可验证的行为，并由对应业务逻辑及所需后端能力支撑；不得用无效点击、假成功、仅 Toast 或静态跳转冒充已完成功能。视觉阶段允许使用隔离 Fixture，但必须明确标注为预览、保持生产禁用，并在视觉确认后继续完成契约、后端与前后端集成；在端到端能力接通前不得宣称该按钮或切片已完成。
